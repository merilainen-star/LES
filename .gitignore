# Build output
dist/

# Dev/debug reference files
search.html

# Build artifacts
*.zip

# Node
node_modules/

# Secrets / PIM Data
LekolarEnhancer/PimData.json
PimData.json
*.json

# Bundled dev tooling (node.exe etc.) — kept locally for scrape-facets.js but not committed
tools/

# Locally-built Edge extension zip (pre-push hook rebuilds it; CI publishes its own)
edge-extension.zip

# Dev artifacts that must never be staged (see .agents/rules.md) — committing these
# nested repos creates bogus gitlinks that break CI's git submodule cleanup
.claude/worktrees/
_push_image_copy/
